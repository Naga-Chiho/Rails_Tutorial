<% content_for(:title, 'Departments/Show' ) %>
<h1><%= @department.name %></h1>

  <ul>
    <% @department.users.each do |user| %>
        <p><%= user.name %></p>
    <% end %>
  </ul>

<%= link_to "一覧に戻る", departments_path %>