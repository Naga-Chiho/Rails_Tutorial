<h1>Users</h1>

<div id="users">
  <%= link_to "新規作成", new_admin_user_path %>

  <% @users.each do |user| %>
      <li style="padding-bottom: 12px;">
        <h3><%= user.name %></h3>
        <%= link_to "詳細", [:admin, user] %>
      </li>
  <% end %>
</div>

<%= paginate @users %>
