<h1>Users</h1>

<div id="users">
  <ul>
    <% @users.each do |user| %>
      <li style="padding-bottom: 12px;">
        <h3><%= user.name %></h3>
        <%= link_to "詳細", user_path(user) %>
      </li>
    <% end %>
  </ul>
</div>

<%= paginate @users %>
